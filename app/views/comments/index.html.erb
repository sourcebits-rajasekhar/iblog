<h1>Listing comments</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Email</th>
    <th>Body</th>
    <th>Article_id</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.id %></td>
    <td><%= comment.name %></td>
    <td><%= comment.email %></td>
    <td><%= comment.body %></td>
    <td><%= comment.article_id %></td>
    <td><%= link_to 'Show', article_comment_path(@article,comment) %></td> -->
    <td><%= link_to 'Edit', edit_article_comment_path(@article, comment) %> |</td>
    <td><%= link_to 'Delete', article_comment_path(@article, comment), :confirm => 'Are you sure?', :method => :delete %>
</td>
  </tr>
<% end %>
</table>

<br />


